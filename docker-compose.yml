services:
  backend:
    build: ./backend
    container_name: url-checker-backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000

  frontend:
    build: ./frontend
    container_name: url-checker-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
